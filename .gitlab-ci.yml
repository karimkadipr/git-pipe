image: node:14.19.0

cache:
  paths:
    - node_modules/

test_async:
  before_script:
    - pwd
    - npm install
    - echo "TRIGGER_PAYLOAD=$TRIGGER_PAYLOAD" >> ".env"
  script:
    - pwd
    - cat ".env"
    - npm run publisher
