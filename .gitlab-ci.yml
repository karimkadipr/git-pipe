image: node:14.19.0

cache:
  paths:
    - node_modules/

test_async:
  before_script:
    # - npm install
    - file "$TRIGGER_PAYLOAD"
    - echo "TRIGGER_PAYLOAD=$TRIGGER_PAYLOAD" >> ".env"
  script:
    - cat ".env"
    # - npm run publisher
